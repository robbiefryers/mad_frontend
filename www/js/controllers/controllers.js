/*when creating module, first parameter is the name of the module,
the second specifies the dependencies for this module*/

/*A controller is a JS function. The job of the controller is to build
a model for the view to display*/

/*This file creates a module called maryhillControllers and all the 
controllers in the separate js files add controllers to this module*/

angular.module('maryhillControllers', [])

